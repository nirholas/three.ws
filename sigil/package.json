{
  "name": "sigil",
  "version": "1.0.0",
  "private": true,
  "description": "Sigil: the open multi-chain vanity address foundry. Grind branded Solana and EVM addresses in the browser, prove how they were made, and deploy the same vanity contract address on every chain.",
  "type": "module",
  "license": "Apache-2.0",
  "author": "nirholas <support@three.ws>",
  "homepage": "https://three.ws/sigil",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nirholas/sigil.git"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "node --test packages/*/test/*.test.js",
    "dev": "npm run dev --workspace @three-ws/sigil-site",
    "dev:api": "npm run dev --workspace @three-ws/sigil-api",
    "build": "npm run build --workspace @three-ws/sigil-site",
    "lint:pack": "node scripts/check-packages.mjs",
    "deploy:pages": "npm run build && npx wrangler pages deploy apps/site/dist --project-name sigil",
    "deploy:worker": "npx wrangler deploy --config apps/api/wrangler.toml"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vite": "^7.3.0"
  }
}
