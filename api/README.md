# API

This directory contains all the backend API endpoints for the 3D-Agent application.

## Structure

The API is structured by modules, with each module handling a specific part of the application's functionality.

## Getting Started

To get started with the API, you will need to...
