/**
 * Boot script for the validation dashboard.
 * Exported as a module to avoid bundler requirement.
 */

import { ValidationDashboard } from '../../src/validation-ui.js';

export { ValidationDashboard };
