# X cookies for the agent's X account (NOT the host account).
# Get them from a logged-in browser: DevTools → Application → Cookies → x.com.
# These tokens grant full account access — store with chmod 600 and rotate after use.
X_AUTH_TOKEN=
X_CT0=
