## Skill

- Slug: `skills/<slug>`
- What it makes an agent do, in one sentence:

## Before and after

One exchange with the skill off, then on:

## Checklist

- [ ] `node tools/validate.mjs` passes and `registry.json` is committed
- [ ] every endpoint, tool and command named in the skill exists and works
- [ ] every script runs (`node scripts/<file>` with the example arguments)
- [ ] nothing moves funds without a preview and the user's explicit confirmation
- [ ] no other crypto project is named or promoted
