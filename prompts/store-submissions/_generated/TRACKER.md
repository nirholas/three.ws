# three.ws Store Submission Tracker

**Rewritten 2026-09-03 from measurements taken that day.** Every row below carries the command
that produced it and what that command actually returned. Nothing was carried forward on trust:
where a prior claim could not be reproduced today it is downgraded here, not repeated.

Production at the time of measurement: `GET https://three.ws/api/version` returned commit
`19906ce52` on Cloud Run revision `three-ws-api-00411-m6h`.

Legend: **not-started** | **blocked** | **ready-to-submit** | **submitted** | **live**

---

## The one finding that gates almost everything

**Vertex AI is denied project-wide on a billing hold, and it is degrading the generation lanes a
reviewer will exercise.** Measured 2026-09-03:

```
gcloud logging read 'resource.type="cloud_run_revision"
  resource.labels.service_name="three-ws-api" (textPayload:"forge" OR textPayload:"vertex")'
  --freshness=2h
```

returns, repeatedly and within the last hour:

- `vertex imagen failed, falling back: Lightning dunning decision is deny for project: projects/93741856042`
- `[forge-reference-image] vertex lane failed, falling through: Vertex reference image returned 403`
- `[llm] chain exhausted: groq=http 429 | openrouter=http 404 | openrouter#2=http 401 | openrouter#3..5=http 404 | nvidia=unreachable (timeout) | ovh=http 429 | vertex-gemini=http 403 | pollinations=http 402 | openai=http 429`
- `[forge] free NVIDIA NIM lane unavailable: NVIDIA returned 504`

Three consequences, all reproduced live today rather than inferred:

1. **The OpenAI B3 blocker is not clearable in code.** The scorer fix IS deployed (`db9fd6191`,
   an ancestor of the live commit) and it works: today's free-lane teapot scored
   `flatness 0.153, thinAxis y, planar: true, reasons:["planar"]`, and
   `shouldEscalateToVisionQA` returned `true` for it. The mesh still shipped, because vision QA
   fails open: rung 1 (Vertex Gemini) is 403 on the billing hold and rung 2 (the free NVIDIA NIM
   VLM) returned 504, so `runQualityGate` answers `qa_available:false` and the router correctly
   ships what it has. The degenerate slab reaches the caller with `score 0.976 / flag ok`.

   **Re-measured 2026-09-09 (task 07), and rung 2 is not dead.** The scorer half re-verified on 40
   consecutive production generations: 4 planar flags, 4 of 4 escalating, 0 false escalations on the
   flatness rule. On the QA half, one live generation at 15:04Z scored **through** the NVIDIA rung
   and returned a full verdict (`provider: nvidia`,
   `model: meta/llama-3.2-11b-vision-instruct`, `score 85`, and a real observation: *"the glass shade
   is slightly too green"*). A second generation two minutes earlier failed open on the Vertex 403.
   So rung 2 is intermittent, not down, and the 504 reading above is no longer true in general. Two
   consequences: the billing hold is still the fix that makes escalation deterministic, and until it
   clears the exposure is a **fraction** of generations rather than all of them. Note also that the
   chain used to report only the FIRST rung's error, so every outage read as "Vertex 403" and rung
   2's own failure was never recorded anywhere; that is fixed (`60111ab16`) and the next reading of
   this section will be able to say why the fallback lost.
2. **The free lane is slow, not broken.** `forge_free` completed in **147s** with a real
   3,674,712-byte GLB. That is the NIM 504 plus fallback latency, not the 2026-07-08 hang.
3. **The OpenRouter rungs are separately misconfigured**, independent of billing: five model IDs
   answer 404 and one answers 401. Restoring those would give the LLM chain a working rung even
   while Vertex is held. Filed here as a finding; it is a generation-lane fix, not a submission
   task.

Owner action, not a code change: clear the GCP billing hold on project `93741856042`.

---

## Which generator owns which artifact

The closing work order asserted that these generators had been retired and that the artifacts were
hand-maintained. They are not, and editing them by hand would be overwritten on the next run:

| Artifact | Generated by | Gate |
|---|---|---|
| `_generated/mcp-listing-source.json` | `node scripts/build-mcp-listing-source.mjs` | `npm run audit:mcp-listing` |
| `_generated/mcp-directories/*.md` | `node prompts/store-submissions/_generated/build-mcp-directory-docs.mjs` | none, run it after any manifest change |
| `_generated/mcp-registry-republish.sh` | `node prompts/store-submissions/_generated/build-registry-republish.mjs` | none, reads the live registry |
| `public/mcp-catalog.json` | `npm run build:mcp-catalog` | `npm run audit:mcp-catalog` |
| `tests/fixtures/mcp-golden-tools.json` | `node scripts/audit-mcp-golden.mjs --update` | `npm run audit:mcp-golden` |

There are **six** MCP gates, not four: `audit:mcp`, `audit:mcp-golden`, `audit:mcp-catalog`,
`audit:mcp-safety`, `audit:mcp-listing` and `audit:mcp-reviewer`.

---

## Submission status

| Target | Status | Verified how, 2026-09-03 | Blocking items |
|---|---|---|---|
| **Claude Connectors Directory** | ready-to-submit (pending the human items) | `POST /api/mcp tools/list` unauthenticated returned **200 with 53 tools in 0.10s**; `initialize` likewise. `GET /.well-known/oauth-protected-resource` 200 with 13 scopes. `/legal/privacy`, `/legal/tos`, `/support`, `/docs/mcp` all 200, and privacy still carries the "MCP Connectors, AI Processing & Payments" section. | The 2026-06-28 blocker that discovery was auth-gated is **resolved and no longer true**: the portal can sync tools with no credential. Remaining: `[HUMAN: Team/Enterprise org + Owner role, or use the public form]` and `[HUMAN: generate a real MCP_REVIEW_SECRET and decide which vendor credentials a reviewer gets]`. Note the 147s free-lane latency above before demoing live. |
| **Claude plugin marketplace** | ready-to-submit | `.claude-plugin/marketplace.json` carries 4 plugins (`three-ws-core`, `three-ws-developer`, `three-ws-pump-fun`, `three-ws-3d`). The end-to-end CLI install evidence from 2026-07-08 stands in `_generated/claude-plugin-marketplace-e2e.md` and was not re-driven today. | `[HUMAN: submit via https://platform.claude.com/plugins/submit per marketplace/plugins/three-ws-3d/SUBMISSION.md]` |
| **Claude Agent Skills pack** | ready-to-submit | `node scripts/build-skills-pack.mjs --check` → `skills-pack manifest up to date (42 skills)`; `.agents/skills/` holds 44 entries. | None in code. The live free-lane E2E it wanted is now green but slow (147s, see above). |
| **OpenAI App Directory** | ready-to-submit (B3 cleared 2026-09-11) | Whole surface re-verified live 2026-09-09: `initialize` 200 keyless, `tools/list` **11 tools** each with its four `openai/*` annotations and correct `outputTemplate`, `resources/list` **2 widget resources** with `widgetCSP`/`widgetDescription`/`widgetDomain`, `/.well-known/3d-studio-openapi.yaml` (Redocly-clean, free-only, all inner URLs 200) and `/.well-known/ai-plugin.json` both 200, `/legal/privacy` + `/legal/tos` 200, a real keyless `forge_free` returned a 5.40 MB GLB in 137s whose `/viewer?src=` returned 200, `POST/GET /api/3d/studio` polled to a real GLB, `/api/ar` correct per device class, and the widget drove through empty/loading/ready/error in a `window.openai`-less browser. Every one of those payloads scanned clean of crypto, payment and internal ids. | **B3 is narrower than it looked.** The scorer fix IS deployed (revision `three-ws-api-00420-ljh`) and re-measuring it on 40 consecutive production generations gave 4 planar flags, 4/4 escalating. The fallback rung is also **not** dead: a 15:04Z generation scored through the free NVIDIA vision lane with a full verdict, so "the NIM fallback 504s" no longer holds in general. **B3 is now closed, and NOT by clearing the billing hold.** Measured 2026-09-11 against revision `three-ws-api-00424-5p4`: ten consecutive authenticated calls to `/api/forge-quality-check` returned **10/10 real verdicts** (`provider: nvidia`, `qa_available: true`), against 3/10 on 2026-09-10 and 0/10 on 2026-09-09. The gate is deterministic again because the vision chain was fixed rather than because Vertex came back: JSON validity became part of the failover test instead of a filter applied after a winner was picked, the per-lane budget stopped being divided equally (which had starved the one healthy rung), a billing-denied Vertex is now parked by a circuit breaker instead of re-probed on every call, and the chain was widened from four providers to ten. Vertex is still denied on the hold; it no longer decides the outcome. The four fixes listed here as unshipped are shipped. Remaining: `[HUMAN: final submit in the partner portal]`. Clearing the GCP billing hold is still worth doing, but it is no longer this row's blocker. |
| **OpenAI GPT Store** | live, with a duplicate to remove | `https://chatgpt.com/g/g-6a563a3b49a88191abf346245491a444-three-ws-3d-studio` returns 200 logged-out. The old draft `g-6a5672fbf3f48191b559e482c7fcbf51` **301s to a 200 page titled "ChatGPT - three.ws 3D Studio"**, so the 2026-09-02 escalation stands: two public GPTs share one name. | `[HUMAN: delete the draft duplicate in My GPTs]` (no API for it) |
| **Official MCP Registry** | ready-to-submit | `node prompts/store-submissions/_generated/build-registry-republish.mjs` against the live registry, after the pagination fix below: **3 servers need a publish (0 stale, 3 new: `herald-mcp`, `knock-mcp`, `home-mcp`), 47 are already current**, including all 7 hosted remotes. Cross-checked with `node scripts/publish-mcp-servers.mjs --dry-run`, which agrees. The batch is staged in `_generated/mcp-registry-republish.sh` and was NOT run. | `[HUMAN: mcp-publisher login github, then review and run the staged batch]` |
| **Smithery** | ready-to-submit | `_generated/mcp-directories/smithery.md` regenerated today from the corrected canonical source; it now covers **50 servers (7 remote + 43 stdio)**. | `[HUMAN: create/claim the Smithery account]` |
| **Glama** | ready-to-submit | `_generated/mcp-directories/glama.md` regenerated today; `glama.json` and `public/.well-known/glama.json` are committed. Glama auto-ingests `io.github.nirholas/*` once the registry batch runs. | `[HUMAN: sign in to Glama as `nirholas` to claim the listing]` |
| **mcp.so** | ready-to-submit | `_generated/mcp-directories/mcp-so.md` regenerated today. | `[HUMAN: create the account]` |
| **PulseMCP** | ready-to-submit | `_generated/mcp-directories/pulsemcp.md` regenerated today. | `[HUMAN: create the account]` |
| **LobeHub** | ready-to-submit | `_generated/mcp-directories/lobehub.md` regenerated today; `public/lobehub/plugin.json` current. | `[HUMAN: create the account]` |

---

## Server and tool inventory, measured today

| Surface | Command | Result |
|---|---|---|
| Hosted remotes | `POST https://three.ws/api/<path> tools/list` (plain `Accept: application/json`, the shape a registry crawler sends) | All 7 answered **HTTP 200 in under 0.12s**: `/api/mcp` 53, `/api/mcp-3d` 35, `/api/mcp-studio` 11, `/api/mcp-agent` 6, `/api/mcp-bazaar` 5, `/api/ibm-mcp` 6, `/api/pump-fun-mcp` 21. Zero hangs. |
| Manifests | `npm run audit:mcp` | **50 MCP manifests consistent** (was 49; `robinhood/hood-mcp` was outside the audit's discovery and is now inside it). |
| Tool contracts | `npm run audit:mcp-golden` | **307** hosted tool contracts match the golden fixture. |
| Public catalog | `npm run audit:mcp-catalog` | catalog matches source: **307** tools across 40 servers. |
| Safety annotations | `npm run audit:mcp-safety` | **307** annotations verified against handler behaviour across **248** files, **6** reviewed exemptions. |
| Canonical listing source | `npm run audit:mcp-listing` | **50 servers (7 remote, 43 stdio)**, up to date. |
| Reviewer path | `npm run audit:mcp-reviewer` | **20 of 20 paid stdio tools returned a clean `PaymentRequired`**; 4 free tools ran; the review entitlement ran the real handler with no charge, and a mismatched secret did not lift the paywall. |
| Reviewer path, hosted remotes | unpaid `tools/call` against every paid tool on `/api/mcp`, `/api/mcp-3d` and `/api/ibm-mcp` | **27 of 27 returned a clean x402 `PaymentRequired`** (`x402Version: 2`, four `accepts` entries), no hang and no credential material in any body. This is the remote surface; the row above is the stdio package. |
| Docs | `npm run audit:docs` | clean (1511 markdown files). |

---

## What changed in the tree today

- **The `mcp://` interop bug is closed, and it was not what the note said.** The official
  `@x402/mcp` auto-pay client handles `mcp://tool/<name>` resource URLs fine: driven against the
  published stdio server it returned `paymentMade: true` and the server answered
  `transaction_simulation_failed`, the correct result for a deliberately unfunded ephemeral buyer.
  What WAS broken on our side is that the bazaar discovery extension declared
  `transport: "stdio"`, a value the spec does not allow (`VALID_MCP_TRANSPORTS` is
  `streamable-http | sse`), so `validateDiscoveryExtensionSpec` rejected every row and an indexer
  would drop it. The field is optional and a stdio server has no valid value for it, so it is now
  omitted in `mcp-server/src/payments.js`, `packages/ibm-x402-mcp/src/payments.js` and
  `examples/paid-mcp-server/src/payments.js`. Re-driven live: `bazaar spec {"valid":true}`,
  `bazaar schema {"valid":true}`. The client half of that claim is now pinned by a second
  re-runnable check, `node scripts/x402-mcp-url-interop-check.mjs`, which stands an in-memory MCP
  server up with upstream's own `createPaymentWrapper` and drives it with a real signer: it exits 0
  today, and a URL rejection would make it throw before any signing with `paymentMade` false.
- **One genuine upstream defect remains, with a runnable reproduction.**
  `node scripts/x402-bazaar-mcp-url-repro.mjs` shows that `@x402/mcp`'s own
  `createToolResourceUrl` mints `mcp://tool/<name>`, and `@x402/extensions`' bazaar
  `extractDiscoveryInfo` canonicalizes it to `"null/<name>"` because `mcp:` is not a WHATWG
  special scheme and `URL.origin` is the string `"null"`. Two packages from one monorepo disagree
  about a URL neither treats as invalid; every MCP row an indexer keys on it collides on `null`.
  The script exits 1 while the bug is live and 0 once it is fixed. Filing it is a human step (it
  posts to an external tracker).
- **The canonical listing source has a generator again.** `scripts/build-mcp-listing-source.mjs`
  reads every factual field from the manifests and keeps only curated copy in its OVERLAY;
  `npm run audit:mcp-listing` (wired into `npm run gate`) fails when the committed JSON drifts. It
  found the file six servers short and thirty-five versions behind. The untracked leftover copy at
  `_generated/build-mcp-listing-source.mjs` was deleted, so one generator owns the file.
- **`robinhood/hood-mcp` is now audited and publishable.** Adding it to the manifest audit
  surfaced two real defects it had been hiding behind the audit's discovery gap: a missing
  `mcpName` in its `package.json` and its absence from `scripts/publish-mcp-servers.mjs`. Both
  fixed; it also gained the `title` every sibling manifest carries. Note its local manifest is
  `0.1.0` while the registry already serves `0.1.1`, so this repo's copy lags the published
  package by a patch.
- **The staged registry batch was overstating the work by 16 publishes, and the cause was
  pagination.** `build-registry-republish.mjs` read a single `limit=100` page of
  `registry.modelcontextprotocol.io`. That page returns 100 VERSION rows covering only ~44 distinct
  server names and hands back a `nextCursor` of `provenance-mcp:0.1.0`, so every server
  alphabetically at or after that cursor was silently classified NEW. That is why the batch listed
  all 7 hosted remotes as needing a publish when the registry already served them. The loader now
  follows the cursor to exhaustion (160 version rows across 72 names), and the batch went from 20
  commands to a correct **3**. The embedded verification snippet had the same one-page flaw and now
  points at `node scripts/publish-mcp-servers.mjs --dry-run`, which queries per server.
- **The free lane is slow but never hangs, and it has a documented escape hatch.** Six back-to-back
  `forge_free` calls returned **HTTP 200 six times out of six**: one inline at 143s with a real
  2,881,160-byte GLB (`glTF` magic, `/viewer?src=` 200), and five that crossed the server's cutoff
  and correctly returned a structured `pending` result carrying a `jobId`. `STUDIO_FORGE_TIMEOUT_MS`
  is unset in production, so the 180,000 ms default in `api/_mcp-studio/forge-client.js` applies.
  Verified end to end: a 205s call's pending handle passed to the **`check_job`** tool returned the
  finished GLB. Section 6 of the submission package now warns that a client with a 60s default
  timeout (the `@x402/mcp` default) should raise it or expect the pending path. Backend choice
  explains the spread: over 24h `forge_creations` shows `hunyuan3d` averaging 94s to done and
  `trellis_selfhost` 235s.
- **`grade_sim_readiness` was failing `audit:mcp-safety`**, declaring `readOnlyHint: true` over a
  `db-write`. The write is `putGrade()`, a read-through cache fill wrapped in try/catch that returns
  null on failure, so the caller receives the same grade either way. Recorded as a reviewed
  category-1 exemption in `scripts/audit-mcp-safety.mjs` rather than weakening the annotation or the
  gate.
- **`packages/home-mcp/server.json` carried a 131-character description** against the registry's
  100-character maximum, which fails `audit:mcp` and would be rejected at publish. Shortened to 93.
  Its five tools also joined the golden fixture and the public catalog, which is why the tool count
  moved from 306 to 307.
- **The submission package's allowed-link list was wrong in both directions.**
  `https://three-ws-public.r2.dev` is referenced by no handler and returns 500, so it was dropped;
  generated assets are served from `https://three.ws/cdn/...`. `https://solscan.io` is the explorer
  the agent registry and persona wallet tools actually emit (58 references) and was **missing**,
  which would have produced confirm prompts for a reviewer. Added, with `https://explorer.solana.com`.
- **Two shipped tool titles carried an em-dash**, which the house rules ban in UI copy: `persona_say`
  and `persona_identity`. Corrected at source in `api/_mcp3d/tools/studio.js`,
  `api/_mcp-studio/persona-tools.js` and `api/_mcp3d/tools/persona-identity.js`. Production serves
  the old punctuation until the next deploy, and the submission package says so explicitly.
- **`npm run audit:mcp-reviewer` is new** (`scripts/audit-mcp-reviewer-path.mjs`). The connector
  reviewer evidence used to be hand-run curl transcripts that went stale the moment a tool landed;
  it is one command now. It immediately found that the "16/16 paid tools" figure was four tools
  out of date.

---

## Prerequisite prompt status

| Prompt | Status | Evidence |
|---|---|---|
| 01 Tool annotation and title audit | complete, superseded | `_generated/tool-inventory.md` + `remote-tools-list.json` are older than the live surface; §4 of `claude-submission.md` supersedes them and was pulled live 2026-09-03. |
| 02 Claude reviewer access guide | complete, refreshed today | `_generated/claude-reviewer-guide.md` and `claude-tool-call-evidence.md` updated to the measured 25-tool surface (5 free + 20 paid), with the audit command that reproduces it. |
| 03 Claude submission package | complete | `_generated/claude-submission.md` regenerated 2026-09-03 against production; independently re-measured here and every number matches (53 and 35 tools, the OAuth metadata, and the 200s). |
| 04 OpenAI free 3D endpoint | complete | 11 tools live, 2 widget resources, keyless generation proven end to end today. |
| 05 OpenAI Apps SDK component | complete | GLB viewer component and its four states; screenshots in `_generated/openai-screenshots/`. |
| 06 OpenAI submission package | package complete, held on B3 | `_generated/openai-submission.md` §0. B3's cause is now known (see the top of this file) and is infrastructure, not code. |
| 07 Embodied live agent avatar | code-complete | Persona tools live on the studio surface; real Postgres and R2 persistence. Screenshots in `_generated/embodiment/` were captured locally and not re-driven today. |
| 08 Live agent commerce | loop real, settlement never demonstrated | The auto-pay path is proven to work end to end (see the interop note above); what has never run is a settlement with real funds. Needs `X402_BUYER_SOLANA_SECRET_BASE58` funded and `MCP_AGENT_TALK_TOKEN` set in production. |
| 09 Conversational remixable 3D | complete; royalty settlement never run | `refine_model` and the lineage core are live. `transferSolanaUSDC` is hardcoded to Solana mainnet with real USDC, so there is no devnet rehearsal for the royalty leg; it is an owner-funding gap, not a code gap. |
| 10 Claude plugin marketplace | complete | 4 installable plugins. |
| 11 Agent Skills pack | complete | 42 skills in the pack manifest, `--check` clean. |
| 12 OpenAI GPT Store Actions | live | Canonical GPT public; Actions lane verified 2026-07-14. Duplicate draft still public (see the table). |
| 13 MCP registries and directories | complete, republish staged | 50 manifests consistent; 20 publishes staged and unrun. |
| 16 Tokenized 3D NFT | code-complete, live mint blocked | `node scripts/tokenize-3d-devnet-e2e.mjs` today: 8 airdrop attempts, all `429 Too Many Requests: "You've either reached your airdrop limit today or the airdrop faucet has run dry"`. Unblock with a pre-funded payer: `E2E_PAYER_SECRET=<base58 devnet secret> node scripts/tokenize-3d-devnet-e2e.mjs`. |
| 17 Embodied on-chain identity | complete; live USDC demo funding-gated | Tools wired into `api/_mcp3d/catalog.js`; a live settle needs a funded wallet. |
| 18 Token-gated 3D embeds | complete | `create_gated_embed` plus the server-verified balance gate. |
| 19 Verifiable AI-3D provenance | complete; live anchor funding-gated | `verify_provenance` free, `anchor_provenance` paid; a real anchor needs a funded issuer key. |
| 20 Spatial MCP standard | complete | Validator, spec, reference renderer, docs. |
| 21 AR-ready exports | complete | `export_ar` plus device-aware launch; `/api/ar` links are emitted by the live free lane (seen in today's `forge_free` response). |

---

## Test-suite state at close-out (2026-09-03)

`npx vitest run`: **28,388 passed, 17 failed across 7 files.** None of the 17 are from this pass, and
the three tests that directly cover the tools changed here pass 46 of 46
(`tests/mcp-studio.test.js`, `tests/mcp3d-persona-identity.test.js`, `tests/mcp-tool-result.test.js`).

| Failing file | Count | Cause |
|---|---|---|
| `tests/audit-guards.test.js` | 2 | Guard-registry drift: `audit:mcp-listing`, `check:macos-icon` and `check:apple-widget` are gate steps or guard-shaped scripts missing from `data/guards.json`. The three commits that introduced them (`1ed0e5c66`, `9527c2ff0`, `58a64ad88`) all landed AFTER the commit carrying this pass's changes, so they cannot be caused by it. |
| `tests/avatar-studio-save.test.js` | 4 | `hydrateAppearance` defaults. Avatar studio, untouched here. |
| `tests/server-404-routes.test.js` | 3 | `.toMatch()` receiving an object. Routing, untouched here. |
| `tests/home-members-ui.test.js` | 3 | Smart-home household panel, untouched here. |
| `tests/home-privacy.test.js` | 1 | Smart-home retention bounds, untouched here. |
| `tests/alpha-drip.test.js` | 3 | Suggested-ladder JSON parsing, untouched here. |
| `tests/no-nul-bytes.test.js` | 1 | A raw NUL byte in `tests/no-duplicate-stylesheet-links.test.js`, another agent's file. |

`tests/api/x402-facilitator-log-durable.test.js` failed on an earlier run of this suite and passes
now: commit `aa121030a` ("build the safe-column fragments on first use, not at import") fixed the
import-time `sql` call in `api/_lib/home/confirm.js` mid-session.

These are left for the agents that own them, per the operating rule that another agent's red does
not stop your green. They do not touch the MCP surface this pack submits.

---

## `[HUMAN: ...]` items, the whole list

1. **Clear the GCP billing hold** on project `93741856042`. It is denying Vertex project-wide, which
   is what leaves the OpenAI B3 blocker unclearable and what is degrading the free generation lane.
2. **Claude org and role.** Team or Enterprise plan with Owner or Directory-management role, or use
   the public form instead of the in-app portal.
3. **Claude reviewer credentials.** Generate a real `MCP_REVIEW_SECRET` value and decide which
   vendor credentials (ideally a scoped, capped `REPLICATE_API_TOKEN`) a reviewer gets for the
   funded path, then paste both into the submission's private reviewer notes. The mechanism is
   built and re-verified today by `npm run audit:mcp-reviewer`.
4. **File the upstream x402 issue** at `https://github.com/x402-foundation/x402/issues/new` with
   the output of `node scripts/x402-bazaar-mcp-url-repro.mjs`. Posting to an external tracker is
   an owner-gated action.
5. **Run the registry batch:** `mcp-publisher login github`, then review and run
   `prompts/store-submissions/_generated/mcp-registry-republish.sh` (**3** publishes: `herald-mcp`, `knock-mcp`, `home-mcp`).
6. **Directory accounts:** Smithery, Glama (sign in as `nirholas` to claim), mcp.so, PulseMCP,
   LobeHub.
7. **Delete the duplicate GPT** `g-6a5672fbf3f48191b559e482c7fcbf51` in ChatGPT's My GPTs; it is
   publicly reachable under the same name as the canonical listing.
8. **Fund a devnet wallet** and run `E2E_PAYER_SECRET=<base58 secret> node scripts/tokenize-3d-devnet-e2e.mjs`.
   The public faucet has rate-limited this host's IP again.
9. **Fund a Solana wallet and set `MCP_AGENT_TALK_TOKEN`** in production to demonstrate a real,
   non-bypassed x402 settlement for the agent-hire loop, and a payout wallet for the remix royalty leg.
10. **Submit, once the above are done:** the Claude Connectors listing, the Claude plugin
    (`https://platform.claude.com/plugins/submit`), and the OpenAI App Directory listing.

---

## Asset kit

Every asset from prompt 14 is present in `_generated/assets/`: `icon.svg`, `icon-512x512.png`,
`icon-256x256.png`, `icon-128x128.png`, seven screenshots (`viewer`, `create`, `studio`,
`validation`, `discover`, `landing`, `embodiment-hero`), and `og-image.png`. What is outstanding is
not a missing file but a re-capture: `openai-submission.md` §4 holds the listing screenshots until a
generation clean enough to photograph exists, which is the same B3 blocker as above.
