# Announcement pack: Paste a contract address, get a payable x402 endpoint

**Surface:** [`/ca2x402`](https://three.ws/ca2x402) · **Stage:** drafted · **Slot:** 2026-10-13 · **Announced externally:** never

Ranked 83 of 322 never-announced surfaces by `npm run announce:rank` (score 59). Drafted on nvidia:moonshotai/kimi-k3 and packed by `npm run announce:kit`, from the evidence brief in [`data/announce-plan/briefs/ca2x402.json`](../../data/announce-plan/briefs/ca2x402.json), against [the announcement voice](../announce-voice.md). Every fact below comes from that brief; nothing in this pack was written from memory.

---

## At a glance

| Field | Value |
|---|---|
| Pack id | `ca2x402` |
| Publish slot | 2026-10-13. The minute is decided at send time from the production schedule seed, so it is not knowable from this repository |
| Lane and pattern | developer / clip |
| Audience | Engineers who will read the code |
| Primary channel | X, @trythreews, through the reviewed content queue |
| Secondary channels | Telegram (@three_ws), the surface's own docs page |
| Proof | https://three.ws/ca2x402 |
| Tracked links | Telegram `https://three.ws/ca2x402?utm_source=telegram&utm_medium=community&utm_campaign=announce-ca2x402&utm_content=announcement`. The X post links the bare URL on purpose: X wraps every link in t.co and the queue's own ledger records the send, so a tagged link buys nothing and reads like marketing. |
| The single CTA | Open /ca2x402 and use it |
| Media | `ca2x402-hero`, motion loop |
| KPI | loop completions and profile visits, then route sessions on the day |

## Why this one, and why now

The ranker scored it on signals measured against our own archive, not on taste:

- **reach (14)**: sitemap priority 0.70, which is what we already decided this surface is worth
- **visual (14)**: renders something worth a frame
- **token (18)**: touches $THREE or the agent economy, the topic band that measured highest in our own archive
- **depth (13)**: documented in the tree, so there is enough to write a mechanism about and link proof for

It shipped on 2026-06-23 and has never been posted about.

The changelog has 3 entries about it, the most recent from 2026-08-16: "CA to x402 now shows the momentum and risk read its own paid endpoint sells".

## The claim, and where it is checked

> Paste any Solana mint or EVM contract address, get back a payable x402 market-intel endpoint with copy-paste snippets, live in the bazaar: https://three.ws/ca2x402

Every checkable part of that is declared as a claim in the queue item, and the verifier re-checks each one against the live surface before the post can be approved:

| Claim | Checked against |
|---|---|
| any Solana mint or EVM contract address | live page shows "Any Solana mint or EVM contract address." |
| a payable x402 market-intel endpoint with copy-paste snippets, live in the bazaar | live page shows "You get a payable x402 endpoint + copy-paste snippets, live in the bazaar." |

## Media

Captured from the live route by `npm run announce:media`, which drives the real page in a real Chromium and writes provenance (route, commit, time, sha256) beside the pixels in [`public/announce/media-manifest.json`](../../public/announce/media-manifest.json).

| Shot | File | Notes |
|---|---|---|
| `ca2x402-hero` | `/announce/img/ca2x402-hero.webp` | Motion loop of the live route. |

**Alt text, required on the post:**

> Screen recording of the three.ws CA to x402 page: a token contract address is pasted into the input, resolved, and a payable x402 market-intel endpoint is generated alongside copy-paste code snippets.

## The post

Pattern: clip. **162 weighted characters**, inside the 100 to 179 band that measured a 3.0x lift. Postable file: [`ca2x402.post.txt`](./ca2x402.post.txt), which is the byte-for-byte source the queue item points at.

```text
Paste any Solana mint or EVM contract address, get back a payable x402 market-intel endpoint with copy-paste snippets, live in the bazaar: https://three.ws/ca2x402
```

### Why it is written that way

The post leads on the mechanism, one pasted address becoming a payable, bazaar-listed x402 endpoint, which is the strongest fact the live page itself states. The revision trims the head into the 100 to 179 band by cutting the route mention and redundant phrasing while keeping both claims quotable from the page.

## Telegram (@three_ws)

The same facts in the channel's longer register. The changelog cron posts release notes there automatically; this is the announcement register, sent by hand or queued alongside the post.

```text
The CA to x402 tool is live at three.ws/ca2x402. Paste any Solana mint or EVM contract address and it generates a payable x402 endpoint serving market intel for that exact token. The endpoint comes with copy-paste snippets for calling it and is listed in the x402 bazaar, so other agents can discover it, with no keys, no backend, and no deploy from you.
```

## Ship it

```bash
npm run announce:media -- --only ca2x402-hero   # capture the frame from the live route
npm run x:content -- review ca2x402               # lint, live fact checks, AI editor
npm run x:content -- run --dry-run --id ca2x402   # exactly what would be sent to X
```

The queue item is `ca2x402` in [`data/x-content/queue.json`](../../data/x-content/queue.json), at status `draft`. It becomes eligible to publish when a passing review record exists and the owner sets its status to `approved`; the Cloud Scheduler tick then sends it at its slot. Nothing here posts on its own.

