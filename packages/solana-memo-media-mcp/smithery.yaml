startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |
    (config) => ({ command: "npx", args: ["-y", "@three-ws/solana-memo-media-mcp"], env: {} })
